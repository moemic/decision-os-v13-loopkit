# Intake facts supplied by Mira (synthetic)

- A volunteer accepts a returned tool and writes its item ID and return time
  in a local ledger.
- A second volunteer checks the condition later; the inspection criteria are
  not included in this source.
- Only an inspected item placed on the lending shelf may be offered for a new
  loan.
- The old desk shorthand `ready` has been used both for `logged in the
  ledger` and for `available to lend`. Mira wants those meanings separated.
- No public wording, safety certification, or automated inventory update is
  authorized by these facts.
