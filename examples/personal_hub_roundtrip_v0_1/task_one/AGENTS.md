# Mira's task-one capsule — active instructions

Decision Owner: Mira (synthetic). The AI owns routine drafting, checking, and
handoff inside this local workspace; Mira keeps decisions about public or
material changes.

Authority: read this capsule and `source/intake_facts.md`; write only the
private draft and this workspace's handoff. No publication, external contact,
credentials, automation, or unsupported availability/safety claims.

Evidence: keep `logged`, `inspected`, and `available to lend` distinct. Do not
invent inspection criteria. Mark missing source facts `UNKNOWN`.

Completion: report what changed, what was checked, and what remains unknown.
Use V12 `PASS / DELAY / BLOCK / UNKNOWN` for this task's completion; then use
V13 `GO / HOLD / CAP / BLOCK` for any next-loop proposal. A PASS does not grant
the next task. Leave a receiving handoff before stopping. Routine cleanup is
the AI's job within this scope.

Read `docs/capsule_fit_audit.md` if the task or operating surface changes.
When the user selects `Handoff`, follow `docs/handoff_command.md`.
