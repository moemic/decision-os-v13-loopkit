# Mira's AI Work Hub (synthetic trial)

Mira is the Decision Owner of this personal copy. Her goal is to keep private,
accurate volunteer instructions for a community tool-lending desk and reuse
what one task teaches in the next. This is a synthetic trial, not a real user.

## Active rules chosen by Mira

- Read `handoff/current_codex_handoff.md` for this hub's current state. The
  upstream `docs/current_signal.md`, trajectory, validation, and pre-fork
  handoffs are reference history, not Mira's goal, Gate, tasks, or permission.
- Work only on local drafts and this trial's local workspaces. Do not publish,
  contact people, deploy, use credentials, or claim facts missing from the
  supplied task source. Ask Mira about a material direction or public decision.
- For a new workspace, follow `docs/new_repo_scaffold_standard.md`: state its
  purpose, select only fitting guards, record `docs/capsule_fit_audit.md`, and
  make a restartable handoff. Existing V13 rules and Field Notes are optional
  references; they do not become active here merely because they are present.
- Keep observations separate from adopted rules. Save a reusable task lesson
  in this hub only when the task evidence supports it. A saved note is not
  authority. Before reusing one, check its trigger and its limits.
- Report what changed, what was verified, and what remains unknown. Use V12
  `PASS / DELAY / BLOCK / UNKNOWN` for completion and V13
  `GO / HOLD / CAP / BLOCK` for a proposed next loop. A completed job never
  authorizes the next job by itself.

The upstream root instructions remain inspectable at fixed source commit
`26c9eead2b627858881ef72d8236ed137c06c5ac` with
`git show <source-commit>:AGENTS.md`. They are reference material, not this
copy's active instructions.
