# V216 bounded delivery and evidence

Repository: `shin4141/decision-os-v13-loopkit`; work branch `codex/v216-aspire-profile-animation`, created from fetched `origin/main` `49b27e521f8a29312a6e0767c4c0f19483d999fb`. The older base `main` worktree was not edited. `c95205772d1ec289f9198a33cfaac68d59409d02` remains the local tip of `codex/recycle-py310-wheel-check` in `/Users/sn/Documents/v13/13-215-distribution-runtime`; `git branch -r --contains` found no remote-tracking branch. It was not merged or used as this branch's base. No V215 re-test was attempted.

Shin's added Aspire is [recorded separately](../docs/v216_public_reuse_aspire.md) and linked from both the roadmap and the ordinary next-work selection entry. At 2026-09-20 02:43 UTC the repository API returned 39 stars; the frozen activity example was captured via GitHub GraphQL at 02:47:24 UTC. Stars, trial and repeat use are not conflated. The profile images are a discovery experiment, not proof of any of the three outcomes.

During the same V216 PR, Shin added the distinct profile purposes: increase `shin4141` followers without a count target or deadline, show cute/playful work alongside rigorous research and repair, and convey breadth/personality to a work visitor. The public GitHub user API returned 37 followers at 2026-09-20 03:04 UTC. This predates the revised animation and is a snapshot only. Stars, followers, actual LoopKit use and work-related outcomes remain separate; no V216 causal effect is observed. The goal record and ordinary selection entry now route this purpose only for separately authorized profile work.

Profile source: `shin4141/shin4141` local checkout at `cc3e38fb2cb4fc3ba339050d30fe1deef9acc425`. The layout generator's inverse substitution of heading and inserted GIF reproduced its entire `README.md` exactly (`True`); achievement prose, evidence links, counts and contact were unchanged. The live profile repo was not written. Cat/fox are newly drawn original icons, with no third-party image/code copied. Renderer is MIT-licensed with the containing repository.

Verification: three renderer/layout tests pass; 8 generic current-state admission tests pass; 56 handoff acceptance tests and 10 handoff CLI tests pass. The separately named `test_13_42_13_43_historical_regression.py` yields 3 failures / 7 tests: it asserts that the *first* block is specifically the V215/reader-entry frontier (including `42e406...`, V215 next action and V215-specific fields). V216 intentionally prepends a different paired candidate; the preserved V215 historical bytes were not edited. Do not weaken or rebaseline this historical test merely to turn the run green; it needs a separately reviewed future-frontier-aware historical assertion before canonical admission. The generic current-state test confirms the paired V216 fields; remote-main admission has **not** occurred.

Visual inspection: first and touch frames show recognizable crown/ears, the dated graph, and local overlay glow; the alternate fox and both heading sizes were inspected. Narrow heading is 440×86 two-line; desktop is 850×62 one-line. Both use pale text on an opaque dark card, readable against a light or dark page; the title remains the exact same words and the alt text provides a text equivalent. No strong blinking was used. Local render and GitHub branch Markdown were inspected, not every browser/device; actual profile visual approval remains open. Image format is GIF per GitHub documentation because inline animated SVG is unsupported.

Remote read-back: pushed two commits to `codex/v216-aspire-profile-animation`; [Draft PR #172](https://github.com/shin4141/decision-os-v13-loopkit/pull/172) was OPEN/Draft with head `8b6913406f828c359596148931d6e1ffc9308289` and base `49b27e521f8a29312a6e0767c4c0f19483d999fb`. GitHub content API returned the 756595-byte cat GIF and profile layout file. In GitHub's rendered Markdown, the crown cat visibly moved and the heading rendered as an image with alt text; at 390px viewport the `picture` source selected `heading_mobile.gif`, at 1024px it selected `heading.gif`. The current profile remains unchanged. No PR checks were reported at this read-back. The final documentation-only follow-up commit requires a fresh head read-back before close.

Practice selection (`execution_id: shin4141/decision-os-v13-loopkit/V216`): For a GitHub profile README with a compact animated heading and arbitrary icon, the documented non-support for inline animated SVG changed the output choice to self-contained GIF, and the narrow-width check led to a two-line `<picture>` source. GitHub branch rendering and `currentSrc` at 390px/1024px show that this route works in the inspected browser; generic cross-browser playback, real-profile behavior, new trials and stars are not observed. Reuse this distinction if a future README animation faces GitHub rendering constraints; re-evaluate with the actual target profile, browser/device and reduced-motion needs. This is one bounded real prototype observation, not FN125 evidence or a claim of adoption.

V12 state: PASS for local bounded prototype and setup, not for upstream adoption or actual user outcomes. V13 gate: HOLD for review. Rollback: revert this branch's additive Aspire routing and prototype before merge, or supersede the admitted route with a new owner decision while preserving the historical declaration. Re-evaluate after Shin visual review, actual first-trial/reuse observation, a change in the declared objective, or unacceptable attention/rendering cost. No release, tag, SNS, new repo, live profile write or automatic next loop.

## Same-PR continuation — revised V216 specification, 2026-09-20

The earlier three failing historical assertions above describe the initial PR state. On re-run before repair, `test_fresh_reader_recovers_the_repaired_frontier_from_first_blocks` required V215-only `reader_ownership_boundary`, `reader_workspace_decision_owner`, `companion_status` and `runtime_evidence_boundary` in V216's first block; `test_repository_check_reads_only_the_new_current_authority` required V215's README next action; `test_post_merge_reader_on_origin_main_recovers_steady_state` required V215's `42e406...` reconstruction base in a cloned V216 first block. Those are current-frontier expectations bound to the prior V215 state, not evidence that the V215 block vanished. The V216 first pair is byte-identical and has the stable fields required by AGENTS. The V215 block below the V216 history boundary is checked against fixed main `49b27e5` including those V215-only fields and text. The older 13-42/13-43 byte hashes and reader-entry boundary checks remain unchanged. No test was deleted, skipped or mechanically rebaselined. The current-state test now explicitly distinguishes an unadmitted work-branch candidate from the exact first pair on fetched `origin/main`; an adopted pair additionally requires its declared reconstruction base to be an ancestor of that remote ref. This PR is still only a candidate.

The primary `crowned_cat.gif` now depicts walk, stop, rapid radial shockwave, horizontal sleep, slow staggered square return, wake and return to the starting pose. The first and last RGB frames match. Each of 196 cells carries its unchanged count and palette level through the motion plan. At burst frame 36 offsets extend in positive and negative x and y; at frame 72 some cells are home and others are still returning; all are home by frame 87, before waking starts at frame 88. The fixed seed selects the stop and each cell's launch/return schedule. The sleeping and stretching cat poses are optional cat-only images; `fox.json` still names one icon image and has the same common loop through tilt/rest. The old touch/glow loop remains one optional pattern. `profile_layout_preview.md` now references the revised main GIF; inverse substitution still reproduces the complete `shin4141/shin4141` README at `cc3e38f` exactly. No native contribution graph or live profile file was modified.

Local verification after this revision: 9 generic current-state tests, 8 historical current-state tests, 4 animation/layout tests, 56 handoff acceptance tests, and 10 handoff CLI tests pass. The heading, narrow `picture` source and prior GitHub branch render remain unchanged in code; the updated GIF and profile mock still require same-PR remote render/read-back after push. Follower baseline and presentation aims are in the V216 goal record and ordinary selection route; the 37-follower snapshot does not establish growth. Star, follower, use and work effects remain separately unobserved for this change.

GitHub branch rendering of the revised GIF visibly showed both the walking/settled image and a scattered-square sleeping frame. This read-back also exposed one cross-repository mock-only link problem: the source profile's relative `MERGE_PORTFOLIO.md` link resolved inside V13 rather than in `shin4141/shin4141`. The layout generator now changes only that target in the review mock to the fixed source commit's portfolio. The visible evidence link works while a reversible inverse still reconstructs the source README exactly; the real profile README was not edited. Recheck the remote mock after the follow-up push.

Follow-up GitHub branch read-back after the link fix showed the full layout preview with its portfolio link resolving to `shin4141/shin4141/blob/cc3e38f.../MERGE_PORTFOLIO.md`; the GitHub content API also found that file at the fixed commit. The revised cat GIF rendered and animated on GitHub. The one-line/two-line `<picture>` markup and heading GIFs are unchanged from the earlier 390px/desktop branch check. This remains a Draft PR candidate; no current-state adoption or live profile publication is inferred. The final closure commit's remote head is to be checked after push.

## V216 visual refinement after Shin's passing-grade review

Shin authorized a further polish on the same PR #172, not a new V number or a profile publication. The previous passing-grade main GIF and its original renderer/assets remain available at immutable commit `31a7624c8e906af24a2f4c480860536237c6af5a` for comparison and rollback. The live GitHub profile icon remains untouched. The new original code-drawn cat has a larger round head, small body and short paws, widely spaced tiny pale eyes, restrained nose/mouth, and a small rounded gold crown without a red ribbon; the curled sleep and half-awake poses are optional cat-only PNGs. The fox config still uses one image and the same common motion.

The primary GIF is generated on one 80-ms-frame timeline, not by concatenating two GIFs. Scene A stops left of center; scene B stops farther right and alone has one five-frame sleep-stir interval during which moving squares hold their positions. Both scenes burst radially, nap while squares return at staggered times, wake after all squares are home, and walk right until the entire sprite and shadow are off-screen. The next scene enters from the left. Walking x advances 13 px per frame on both wraps; common ground height, awake sprite size and the position-based gait phase carry across the off-screen wrap. The decoded GIF was expanded by its encoded durations and inspected as two consecutive cycles: A→B and B→A retain pixel-identical background/base-cell regions (outside the entering sprite), the first entering frame differs from the final exit frame, and the encoded empty handoff is no more than two 80-ms ticks. This removes a conspicuous visible reset, not all perceptibility of repetition. Cell counts and their original positions are invariant; motion remains decoration.

An intermediate encoding exposed a real seam defect: default error-diffusion dithering changed otherwise identical squares when a sprite entered at the far left. Disabling dither while retaining one fixed palette made both decoded seam backgrounds pixel-identical and reduced the new cat GIF from about 3.2 MB to 2,236,406 bytes. The fox GIF is 2,181,311 bytes; durations are 19.84 s and 19.76 s respectively. The heading GIFs remain opaque dark cards with bright text; desktop/mobile sizes are unchanged and now encode without seam-wide dither noise. Local renderer tests cover two-cycle seams, pace/phase/clip, five-frame B pause, deterministic frames, data integrity, encoded frame durations/capacity and the profile body inverse. Five renderer/layout tests, nine generic admission tests, eight historical current-state tests, 56 handoff acceptance tests and 10 handoff CLI tests pass. The two-cycle seam frames were decoded and visually inspected as a contact sheet; continuous browser playback for two full cycles is not yet claimed. Live GitHub rendering and narrow-width confirmation of this final version remain to be checked after the PR push.

The V13-star and profile-follower/presentation purposes, comparison boundaries, evidence links, counts and contact remain as recorded above. No increase in stars/followers, actual LoopKit use or work effect is attributed to the GIF. No main or live profile write is authorized by this result.
